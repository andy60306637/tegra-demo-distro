# extlinux.conf FDT/OVERLAYS 問題修復總結

**日期**: 2025-11-27  
**問題**: 編譯錯誤 - `l4t-launcher-extlinux` 無法找到 DTB 檔案  
**狀態**: ✅ 已修復

---

## 問題根因

meta-tegra 的 `l4t-launcher-extlinux.bb` recipe 的 `do_install` 函數不支援 `devicetree/` 子目錄路徑:

```bash
# 原始 do_install 假設檔案在 ${B}/devicetree/tegra234-...dtb
# 但 do_copy_dtb_overlays 實際複製到 ${B}/tegra234-...dtb (無子目錄)
install -m 0644 ${B}/${L4T_UBOOT_EXTLINUX_FDT}* ${D}${L4T_EXTLINUX_BASEDIR}/
```

當 `L4T_UBOOT_EXTLINUX_FDT = "devicetree/tegra234-...dtb"` 時:
- 試圖從 `${B}/devicetree/tegra234-...dtb` 安裝 → **找不到檔案** ❌
- 實際檔案位於 `${B}/tegra234-...dtb` → **路徑不匹配** ❌

---

## 解決方案

### 1. 修改配置 (project.yml)

```yaml
local_conf_header:
  my-project: |
    # ... 其他設定 ...
    
    # extlinux.conf configuration for FDT and camera overlay
    UBOOT_EXTLINUX_FDT = "devicetree/tegra234-p3768-0000+p3767-0004-nv-super.dtb"
    UBOOT_EXTLINUX_FDTOVERLAYS = "devicetree/tegra234-p3767-camera-p3768-imx219-C.dtbo"
```

### 2. 建立 bbappend 修正安裝邏輯

檔案: `layers/meta-tegrademo/recipes-bsp/uefi/l4t-launcher-extlinux.bbappend`

核心邏輯:
```bash
if [ -n "${L4T_UBOOT_EXTLINUX_FDT}" ]; then
    case "${L4T_UBOOT_EXTLINUX_FDT}" in
        devicetree/*)
            # 建立 devicetree/ 子目錄
            install -d ${D}${L4T_EXTLINUX_BASEDIR}/devicetree
            # 取得檔名 (無目錄前綴)
            dtb_basename=$(basename ${L4T_UBOOT_EXTLINUX_FDT})
            # 從 ${B}/檔名 安裝到 ${D}/boot/devicetree/檔名
            install -m 0644 ${B}/${dtb_basename}* ${D}${L4T_EXTLINUX_BASEDIR}/devicetree/
            ;;
        *)
            # 無 devicetree/ 前綴,直接安裝到 /boot/
            install -m 0644 ${B}/${L4T_UBOOT_EXTLINUX_FDT}* ${D}${L4T_EXTLINUX_BASEDIR}/
            ;;
    esac
fi
```

**原理**:
1. 檢查路徑是否以 `devicetree/` 開頭
2. 如果是:
   - 建立目標目錄 `${D}/boot/devicetree/`
   - 使用 `basename` 取得檔名 (去除 `devicetree/` 前綴)
   - 從 `${B}/tegra234-...dtb` 安裝到 `${D}/boot/devicetree/tegra234-...dtb`
3. 如果不是:
   - 保持原始邏輯 (向後相容)

---

## 驗證

### 1. 檢查 bbappend 是否被識別

```bash
cd build
bitbake-layers show-appends | grep l4t-launcher-extlinux
```

✅ 應顯示:
```
l4t-launcher-extlinux.bb:
  /home/andy/proj/tegra-demo-distro/layers/meta-tegrademo/recipes-bsp/uefi/l4t-launcher-extlinux.bbappend
```

### 2. 清理並重新編譯

```bash
cd build
source ../layers/oe-init-build-env .
bitbake -c cleansstate l4t-launcher-extlinux
cd ..
kas build project.yml
```

### 3. 檢查安裝結果

編譯完成後,檢查工作目錄:

```bash
ls -la build/tmp-glibc/work/p3768_0000_p3767_0004-oe-linux/l4t-launcher-extlinux/1.0/image/boot/devicetree/
```

✅ 應包含:
- `tegra234-p3768-0000+p3767-0004-nv-super.dtb`
- `tegra234-p3767-camera-p3768-imx219-C.dtbo`

### 4. 驗證 extlinux.conf

```bash
cat build/tmp-glibc/work/p3768_0000_p3767_0004-oe-linux/l4t-launcher-extlinux/1.0/l4t-launcher-extlinux-1.0/extlinux.conf
```

✅ 應包含:
```
FDT /boot/devicetree/tegra234-p3768-0000+p3767-0004-nv-super.dtb
OVERLAYS /boot/devicetree/tegra234-p3767-camera-p3768-imx219-C.dtbo
```

---

## 編譯錯誤對比

### 修復前 ❌

```
ERROR: l4t-launcher-extlinux-1.0-r0 do_install: ExecutionError(...)
install: cannot stat '/home/andy/proj/tegra-demo-distro/build/tmp-glibc/work/.../l4t-launcher-extlinux-1.0/devicetree/tegra234-p3768-0000+p3767-0004-nv-super.dtb*': No such file or directory
```

**問題**: 試圖從 `${B}/devicetree/...` 安裝,但檔案在 `${B}/...`

### 修復後 ✅

```
NOTE: recipe l4t-launcher-extlinux-1.0-r0: task do_install: Started
NOTE: recipe l4t-launcher-extlinux-1.0-r0: task do_install: Succeeded
```

**結果**: 成功建立 `/boot/devicetree/` 並安裝檔案

---

## 最終檔案結構

### ESP 分區 (/boot/)

```
/boot/
├── extlinux/
│   └── extlinux.conf          ← 包含 FDT 和 OVERLAYS 設定
├── Image                       ← Kernel
├── initrd                      ← InitRAMFS
└── devicetree/                 ← DTB/DTBO 目錄 ✅
    ├── tegra234-p3768-0000+p3767-0004-nv-super.dtb      ← 主 DTB ✅
    ├── tegra234-p3767-camera-p3768-imx219-C.dtbo        ← IMX219 DTBO ✅
    └── ... (其他 DTB/DTBO)
```

### extlinux.conf 內容

```
# L4TLauncher configuration file generated by OE4T
MENU TITLE L4T boot options
DEFAULT primary
LABEL primary
        MENU LABEL primary
        LINUX /boot/Image
        INITRD /boot/initrd
        FDT /boot/devicetree/tegra234-p3768-0000+p3767-0004-nv-super.dtb     ← ✅
        OVERLAYS /boot/devicetree/tegra234-p3767-camera-p3768-imx219-C.dtbo  ← ✅
        APPEND ${cbootargs} mminit_loglevel=4 console=ttyTCU0,115200 firmware_class.path=/etc/firmware fbcon=map:0 nospectre_bhb console=tty0
```

---

## 相關檔案

1. **project.yml** - KAS 配置 (已修改)
2. **layers/meta-tegrademo/recipes-bsp/uefi/l4t-launcher-extlinux.bbappend** - 安裝邏輯修正 (新建)
3. **repos/meta-tegra/recipes-bsp/uefi/l4t-launcher-extlinux.bb** - 原始 recipe (未修改)
4. **extlinux-fdt-overlays-configuration-guide.md** - 完整說明文件

---

## 總結

✅ **問題已解決** - 透過建立 bbappend 覆蓋 `do_install` 函數  
✅ **編譯成功** - `l4t-launcher-extlinux` 正確安裝 DTB/DTBO 到 `/boot/devicetree/`  
✅ **extlinux.conf 正確** - 包含 FDT 和 OVERLAYS 設定  
✅ **向後相容** - 支援有/無 `devicetree/` 前綴的路徑  

下一步: 完成 `demo-image-egl` 編譯並燒錄測試 🚀
